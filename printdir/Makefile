PRINTDIR_SOURCE=printdir.cpp
PRINTDIR_OUTPUT=printdir-test
CFLAGS=-std=c++11

all: $(PRINTDIR_OUTPUT)

$(PRINTDIR_OUTPUT): $(PRINTDIR_SOURCE)
	$(CXX) $(CFLAGS) $(PRINTDIR_SOURCE) -o $(PRINTDIR_OUTPUT)
clean:
	rm -f printdir-test
